# Console

## Convenience API for expert users

###
These functions can be used from the 'mv' object in the browser-console:
- getAllScores()

Maintenance functions:
- getScoresByUser(userName)
- deleteScoresByUser(userName)
Individual scores can be deleted from score-detail view in the Hall of Fame.

Statistical functions:
- addSampleScores()
- deleteSampleScores()
